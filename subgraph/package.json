{
  "name": "subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:studio": "graph deploy --studio subgraph",
    "deploy:docker": "graph deploy portley-0/liveduel-tournament subgraph.yaml --node http://localhost:8020 --ipfs http://localhost:5001",
    "test": "graph test",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.96.0",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
